using System; 

class URI {

    static void Main(string[] args) { 

        int A,B,SOMA;
        
        A = int.Parse(Console.Read());
        B = int.Parsr(Console.Read());
        SOMA = A + B;
        
        Console.WriteLine("SOMA = {0}",SOMA);

    }

}